import { H2, P } from '@rr/ui';

export default function FooComponent() {
  return (
    <>
      <H2 className="mt-3"> Step 1 - Generate new breadcrumbs scaffold:</H2>
      <P>
        Use the NX generator to create a new app. It should include the base
        config and setup.
      </P>
    </>
  );
}
